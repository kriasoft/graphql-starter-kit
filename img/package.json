{
  "name": "img",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deploy": "node ../scripts/deploy-img",
    "img:deploy": "yarn workspace img run deploy"
  },
  "dependencies": {
    "image-resizing": "^0.1.3"
  }
}
